{
  "name": "with-styled-components",
  "version": "1.0.0",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "deploy": "rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && git add out/ && git commit -m 'New Deployment' && git push origin `git subtree split --prefix out development`:master --force",
    "save": "git add . && git commit -m 'updates' && git push"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "googleapis": "^42.0.0",
    "lottie-web": "^5.5.7",
    "next": "latest",
    "nodemailer": "^6.3.0",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-lottie": "^1.2.3",
    "styled-components": "^4.0.0"
  },
  "devDependencies": {
    "@zeit/next-css": "^1.0.1",
    "babel-plugin-styled-components": "^1.8.0",
    "next-compose-plugins": "^2.2.0",
    "next-images": "^1.1.2"
  },
  "license": "ISC"
}
